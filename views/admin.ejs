<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Futuristic Admin Panel</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <style>
        body {
            height: 100vh;
            margin: 0;
            overflow: hidden;
            display: flex;
            justify-content: center;
            align-items: center;
            background: #000;
            position: relative;
        }

        #particles-js {
            position: absolute;
            width: 100%;
            height: 100%;
            z-index: 0;
        }

        .card {
            position: relative;
            z-index: 1;
            background: rgba(255, 255, 255, 0.1);
            border: 2px solid rgba(255, 255, 255, 0.2);
            border-radius: 20px;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.8);
            backdrop-filter: blur(10px);
            padding: 40px;
            max-width: 500px;
            width: 100%;
            text-align: center;
            color: #fff;
        }

        h1 {
            background: linear-gradient(90deg, #00c6ff, #0072ff);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .btn-custom {
            background: linear-gradient(135deg, #00c6ff, #0072ff);
            color: #fff;
            border: none;
            border-radius: 30px;
            padding: 10px 20px;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .btn-custom:hover {
            transform: scale(1.1);
            box-shadow: 0 4px 15px rgba(0, 198, 255, 0.8);
        }

        .btn-danger {
            background: linear-gradient(135deg, #ff416c, #ff4b2b);
            color: #fff;
            border: none;
            border-radius: 30px;
            padding: 10px 20px;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .btn-danger:hover {
            transform: scale(1.1);
            box-shadow: 0 4px 15px rgba(255, 65, 108, 0.8);
        }

        .futuristic-btn {
        background: linear-gradient(135deg, #00c6ff, #0072ff); /* Vibrant Gradient */
        color: #fff;
        border: none;
        border-radius: 50px;         /* Smooth rounded edges */
        padding: 12px 30px;         /* Balanced padding */
        font-weight: 600;           
        letter-spacing: 1px;         /* Stylish letter spacing */
        transition: all 0.3s ease;   /* Smooth transition */
        box-shadow: 0 4px 15px rgba(0, 114, 255, 0.4); /* Glowing effect */
        position: relative;
        overflow: hidden;            /* Prevents shine overflow */
        z-index: 1;
        
    }

    .futuristic-btn::before {
        content: "";
        position: absolute;
        top: -100%;
        left: -100%;
        width: 300%;
        height: 300%;
        background: rgba(255, 255, 255, 0.3); /* Shine effect */
        transform: rotate(45deg);
        transition: all 0.5s ease;
        z-index: -1;
    }

    .futuristic-btn:hover::before {
        top: 0;
        left: 0;
    }

    .futuristic-btn:hover {
        box-shadow: 0 6px 20px rgba(0, 114, 255, 0.7); /* Intense glow on hover */
        transform: translateY(-2px); /* Slight hover lift */
    }
    </style>
</head>

<body>

<div id="particles-js"></div>

<div class="card translucent-card  shadow-lg border-0 rounded-4 overflow-hidden ">
    <h1>Admin Panel</h1>
    <div class="d-flex justify-content-center my-3">
        <a href="/students" class="btn futuristic-btn">Manage Students</a>
        <a href="/courses" class="btn futuristic-btn">Manage Courses</a>
        <a href="/enrollments" class="btn futuristic-btn">Manage Enrollments</a>
    </div>
    <form action="/auth/logout" method="POST" style="display: inline;">
        <button type="submit" class="btn btn-danger">Logout</button>
    </form>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
<script src="https://cdn.jsdelivr.net/particles.js/2.0.0/particles.min.js"></script>
<script>
    particlesJS("particles-js", {
        particles: {
            number: { value: 100, density: { enable: true, value_area: 800 } },
            color: { value: "#00c6ff" },
            shape: { type: "circle" },
            opacity: { value: 0.5, random: true },
            size: { value: 4, random: true },
            move: { enable: true, speed: 3 }
        }
    });
</script>

</body>

</html>
